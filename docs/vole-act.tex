\documentclass[11pt]{article}

\usepackage[margin=1.1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{xcolor}
\definecolor{linkblue}{RGB}{25,60,120}
\usepackage[operators]{cryptocode}
\usepackage{float}
\usepackage{placeins}
\usepackage[colorlinks=true,linkcolor=linkblue,citecolor=linkblue,urlcolor=linkblue]{hyperref}

\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
\newtheorem{claim}{Claim}
\newtheorem{lemma}{Lemma}
\newtheorem{remark}{Remark}

\newcommand{\F}{\mathbb{F}}
\newcommand{\Ftwo}{\F_2}
\newcommand{\Fsixteen}{\F_{16}}
\newcommand{\Ftag}{\F_{2^{128}}}
\newcommand{\shake}{\mathsf{SHAKE256}}
\newcommand{\ctx}{\mathsf{ctx}}
\newcommand{\Spre}{\mathsf{SPre}}
\newcommand{\enc}{\mathsf{enc}_{64}}
\newcommand{\Cred}{\mathsf{C}}
\newcommand{\Null}{\mathsf{N}}
\newcommand{\Def}{\mathsf{D}}
\newcommand{\adv}{\mathcal{A}}

\title{VOLE-ACT: Post-Quantum Anonymous Credit Tokens\\
from VOLE-in-the-Head and MAYO}
\author{Samuel Schlesinger}
\date{July 2026\\[0.5em]\normalsize Research draft --- definitions and analysis are stated as targets;\\
no complete security reduction is claimed}

\begin{document}
\maketitle

\begin{abstract}
Anonymous credit tokens let an issuer grant a client a hidden, spendable
balance: the client can later redeem credits unlinkably, each redemption
consumes a one-time nullifier and returns a fresh token on the remaining
balance. Classical constructions rest on discrete-logarithm-style algebra ---
blind or randomizable signatures, algebraic MACs, Sigma protocols --- none of
which survives a quantum adversary. We present VOLE-ACT, a post-quantum
candidate instantiation of the Katz--Schlesinger anonymous-credit design~\cite{KS}
whose entire symmetric layer is Keccak and whose only algebraic assumption
concerns the multivariate-quadratic (whipped MAYO) trapdoor. The central
idea is to \emph{avoid blindness entirely}: after accepting a revealed
SHAKE256 commitment digest, the issuer salts and signs a wrapper target;
possession of the signature is later proven in zero knowledge inside a VOLE-in-the-Head
proof system that is native to exactly the two ingredients that must be
proven --- Keccak (a Boolean circuit) and MAYO verification (a quadratic
system over $\Fsixteen \subseteq \Ftag$). We give syntax and security
definitions for anonymous credit tokens, the construction --- including a
degree-16 Keccak checkpointing technique that commits the permutation state
only every four rounds, and a batched quadratic-system constraint for
whipped MQ verification --- and a security analysis that states its
assumptions (the ordinary MAYO OV and Multi-Target Whipped MQ problems; an
ideal-XOF model) and its concrete attack ceilings explicitly. A Rust
research prototype achieves 29--31\,ms client proving per spend across the
three proof-size profiles and 9.2--9.4\,ms issuer verify-and-sign (proof
verification plus MAYO preimage sampling) in the Balanced profile, with
71\,KiB compact-profile proofs and 315-byte tokens.
\end{abstract}

\section{Introduction}

An anonymous credit token system has one issuer and many clients. The issuer
grants a client a public number of credits; the client can later spend part of
that balance without revealing which issuance --- or which earlier spend ---
produced the token being presented. Each accepted spend consumes a one-time
nullifier (preventing double spending) and returns a fresh token carrying the
remaining balance. The functionality generalizes anonymous tokens in the
Privacy Pass style~\cite{PrivacyPass} from unit tokens to stateful balances,
and specializes keyed-verification anonymous
credentials~\cite{CMZ14} to a single numeric attribute with exact arithmetic.

Classically this is well-trodden ground: blind signatures~\cite{Chaum82},
CL~signatures~\cite{CL01}, algebraic MACs~\cite{CMZ14} and their pairing-based
relatives give compact, efficient constructions. All of them lean on the
algebraic structure of discrete-log or pairing groups, both for the signature
and for the efficient zero-knowledge presentation protocol. Neither survives
a quantum adversary. The natural repair --- a post-quantum \emph{blind}
signature --- remains an active and unsettled research area in which several
early lattice-based proposals required substantial revision after
cryptanalysis; we prefer a design that does not need blindness at all.

\paragraph{The core idea: sign a digest, hide the preimage.}
VOLE-ACT, following the Katz--Schlesinger design~\cite{KS}, sidesteps
blindness. A credential is a short XOF stream
\[
X \;=\; \shake(\text{domain} \,\|\, \ctx \,\|\, \kappa \,\|\, \enc(b) \,\|\, \rho),
\]
over a client-chosen 256-bit nullifier key $\kappa$, public balance $b$, and
256-bit hiding nonce $\rho$. The first $4m$ bits of $X$ form the
\emph{commitment digest} $\Cred \in \Fsixteen^m$; the next 256 bits form the
\emph{nullifier} $\Null$. At issuance the client \emph{reveals} $\Cred$ and
proves in zero knowledge that it is well-formed for the public balance $b$;
the issuer then chooses a fresh 256-bit salt $\zeta$, computes
$Y=\shake(\text{``signed''}\|\mathsf{pack16}(\Cred)\|\enc(0)\|\zeta)[0{:}4m]$,
and returns a MAYO preimage $\sigma \gets \Spre(\mathsf{sk},Y)$ together
with $\zeta$. Nothing is blind: the issuer sees the commitment it
authenticates. Unlinkability comes later --- at
spend time the client proves \emph{in zero knowledge} that it knows
$(\sigma, \kappa, b, \rho,\zeta)$ with
$P^\ast(\sigma)=\mathsf{SignedTarget}(\Cred(\kappa,b,\rho),0,\zeta)$,
revealing only the nullifier $\Null$, the public spend amount, and a fresh
commitment digest $\Cred'$ for the successor token. The signature and the
signed digest are never shown again, so there is nothing to link. The
unrevealed suffix of $X$ is treated as pseudorandom given its prefix, which
is what makes the nullifier unlinkable to the issuance in which the issuer
saw $\Cred$.

This pattern reduces the required primitives to (i) a post-quantum trapdoor
map whose \emph{verification} is cheap inside a proof system, and (ii) a
post-quantum zero-knowledge proof system that is efficient on
\emph{hash-heavy Boolean} statements. The pairing of MAYO with
VOLE-in-the-Head is unusually harmonious on both counts:

\begin{itemize}
  \item \textbf{MAYO}~\cite{Beullens21,MAYOspec} verification evaluates a
  public multivariate quadratic map over $\Fsixteen$. Since
  $\Fsixteen$ embeds as a subfield of the proof system's tag field
  $\Ftag$, the whole verification equation becomes a single batched
  \emph{quadratic system} constraint in the proof --- no bit decomposition,
  no modular arithmetic emulation.
  \item \textbf{VOLE-in-the-Head}~\cite{VOLEitH,FAEST} proof systems commit
  witness \emph{bits} and prove Boolean and low-degree relations at
  QuickSilver~\cite{QuickSilver,Wolverine} cost: linear gates are free, and
  the entire batch of polynomial constraints of maximum degree $d$ is checked
  with $d$ published $\Ftag$ elements. Keccak --- the only hash used
  anywhere --- is the friendliest possible circuit for such a system: its
  round function is degree 2.
\end{itemize}

\paragraph{Contributions.}
(1)~Syntax and game-based security definitions for anonymous credit tokens
with issuer-settled (\emph{deferred-return}) spends
(Section~\ref{sec:definitions}), together with an explicit account of why
a signer-salted target restores the ordinary MAYO proof strategy and avoids
an adaptive \emph{one-more-preimage} oracle assumption.
(2)~A post-quantum candidate construction from the ordinary MAYO foundation plus an ideal XOF
(Section~\ref{sec:scheme}): one XOF stream yielding both commitment and
nullifier; a typed two-format token state machine; and a deferred-return
settlement in which the issuer supplies a bounded return after the client fixes
its proved request and before signer-salt generation, bound by the common hash.
(3)~Proof-circuit techniques (Section~\ref{sec:circuit}): \emph{degree-16
Keccak checkpointing}, committing the permutation state only every four
rounds and quartering the dominant witness cost by spending the proof
system's polynomial-degree budget; and a batched quadratic-system constraint
expressing whipped MQ verification over shared product terms, folded by
verifier randomness.
(4)~A security analysis (Section~\ref{sec:security}) that is explicit about
its conditional status, its concrete error terms, and its generic attack
ceilings --- including a $2^{256/3}$-query quantum collision bound on the
credential digest.
(5)~Evidence of practicality: a Rust research prototype
(Section~\ref{sec:performance}) with 29--31\,ms spend proving across profiles,
9.2--9.4\,ms Balanced-profile issuer verify-and-sign, 71\,KiB compact proofs,
and 315-byte tokens.

\section{Preliminaries}

\subsection{Notation}
$\lambda = 128$ throughout. $\Ftwo$, $\Fsixteen = \Ftwo[x]/(x^4{+}x{+}1)$ and
$\Ftag = \Ftwo[x]/(x^{128}{+}x^7{+}x^2{+}x{+}1)$ are the Boolean, MAYO, and
tag fields; $\iota : \Fsixteen \hookrightarrow \Ftag$ is the
protocol-specified field embedding (the subfield is unique; the embedding is
fixed deterministically, with the reduction polynomial family used by
FAEST~\cite{FAEST}). $\shake$ is used for every PRG, commitment, and
Fiat--Shamir hash, always with prefix-free domain separation and
fixed-width or length-framed inputs~\cite{FIPS202}. $\enc(\cdot)$ is 64-bit
little-endian encoding. $\kappa$ denotes the client's nullifier key, $h$ a
GGM tree depth, and $k_w$ MAYO's whipping parameter.

\subsection{VOLE-based zero knowledge and VOLE-in-the-Head}
\label{sec:voleith}

In VOLE-based ZK~\cite{Wolverine,QuickSilver}, the prover holds, for each
witness bit $u_t \in \{0,1\}$, a \emph{tag} $V_t \in \Ftag$, and the verifier
holds a global secret $\Delta \in \Ftag$ and per-coordinate \emph{keys}
\[
K_t \;=\; V_t + u_t \cdot \Delta ,
\]
a vector oblivious linear evaluation (VOLE) correlation. Wires are affine
combinations of committed bits and are linearly homomorphic for free. A
multiplication (degree-2) constraint $a \cdot b = c$ is checked
QuickSilver-style: the verifier forms $B = K_a K_b + \Delta K_c$, which the
prover can match with a degree-1 polynomial in $\Delta$ exactly when the
constraint holds; a random linear combination with challenge weights
$\chi_i$ batches all constraints into one polynomial identity checked at
$\Delta$. The generalization to degree-$d$ polynomial constraints costs
$d$ published coefficients and $d-1$ random VOLE masks in total for the whole
batch: mask $j \in \{0,\dots,d-2\}$ is a tag-field VOLE pair contributing
the degree-1 polynomial $(v_j + u_j\Delta)\,\Delta^{j}$, of degree at most
$d-1$ in total, so the leading, constraint-carrying coefficient is left
untouched. Lemma~\ref{lem:mask} makes the masking exact: the published
vector releases the check equation and nothing else.

\begin{lemma}[Coefficient masking]
\label{lem:mask}
Fix $\Delta$ and the mask keys $K_j = v_j + u_j\Delta$ for
$j \in \{0,\dots,d{-}2\}$ (each mask group packed into one $\Ftag$
element), and let the honest prover publish
$c = (c_0,\dots,c_{d-1}) \in \Ftag^{d}$, the coefficient vector of its
masked response polynomial. Conditioned on the rest of the verifier's
view, $c$ is uniform on the affine hyperplane
$\{\,c : \sum_{i<d} c_i \Delta^{i} = A\,\}$, where $A$ is the value the
verifier computes for the check equation.
\end{lemma}

\begin{proof}
The blinds added to the unmasked coefficients are
$b_0 = v_0$, $b_j = v_j + u_{j-1}$ for $1 \le j \le d-2$, and
$b_{d-1} = u_{d-2}$. Substituting $v_j = K_j + u_j\Delta$
(characteristic 2) makes $b$ an affine function of
$(u_0,\dots,u_{d-2})$, injective by back-substitution: $b_{d-1}$
determines $u_{d-2}$, then each $b_j$ determines $u_{j-1}$ from $u_j$,
and $b_0$ is then fixed. Its evaluation is constant:
$\sum_j b_j \Delta^{j} = \sum_j (v_j + u_j\Delta)\Delta^{j} =
\sum_j K_j \Delta^{j}$, known to the verifier. So the image is exactly
the hyperplane $\{\,b : \sum_j b_j\Delta^{j} = \sum_j K_j\Delta^{j}\,\}$
of size $|\Ftag|^{d-1}$, an injective affine image of
$(u_0,\dots,u_{d-2}) \in \Ftag^{d-1}$ --- which stays uniform given the
keys, since each pair $(u_j, v_j)$ is uniform on the line
$v_j = K_j + u_j\Delta$ --- hence uniform on it; translating
by the fixed unmasked coefficients preserves uniformity on the shifted
hyperplane, whose defining value is the check value $A$.
\end{proof}

VOLE-in-the-Head~\cite{VOLEitH} makes this \emph{publicly verifiable and
non-interactive}: the prover generates the VOLE correlation itself from
$\tau$ GGM~\cite{GGM86} seed trees of depth $h$ (with $\tau \cdot h =
\lambda$), commits to all $2^h$ leaves of each tree with an all-but-one
vector commitment, and the Fiat--Shamir challenge $\Delta$ selects, per tree,
one leaf that stays hidden; the remaining leaves are opened and the verifier
recomputes its keys. Soundness comes from the hidden leaf; zero knowledge
from the fact that the opened leaves are exactly what the verifier can
recompute anyway. Our instantiation follows the hardening of
FAEST~\cite{FAEST} and ``One Tree to Rule Them All''~\cite{OneTree}: every
PRG and commitment call binds a per-proof $2\lambda$-bit salt, a per-tree
derived salt, and the exact tree position, preventing the known multi-target
and multi-instance amortization attacks on the 128-bit seeds; small-field
consistency across the $\tau$ trees uses public corrections re-based onto
one shared $u$-vector, in the style of SoftSpokenOT~\cite{SoftSpoken}.

Three parameter profiles trade proof size against tree-expansion work:
\emph{compact} $(\tau,h) = (16,8)$, \emph{balanced} $(32,4)$, and
\emph{low-latency} $(64,2)$. Each opened proof reveals $\tau$ tree openings
of $h$ sibling seeds, so proofs shrink as $h$ grows while the prover expands
$\tau \cdot 2^h$ leaves.

\paragraph{The consistency check must be a matrix hash.}
Because each tree contributes only $h$ bits of $\Delta$, a cheating prover
who mangles one tree's correction data is caught by a per-tree check only
with probability $1 - 2^{-h}$ --- as low as $3/4$ for $h=2$. The
construction therefore applies a $128$-row \emph{column-wise linear
universal hash} to the entire tag matrix (two independent polynomial
evaluations combined with random weights, plus a one-time-pad block), so
that any inconsistency survives into the check equation except with
probability at most $2^{-\lambda} + \bigl((L{-}1)/2^{\lambda}\bigr)^{2}$
per column for $L$ hashed chunks (Lemma~\ref{lem:hash}; $L \approx 186$
for a direct spend, making the second term $\approx 2^{-241}$),
regardless of the chunk structure. Composing this per-column bound with
the challenge schedule and the Fiat--Shamir transform is part of the
outstanding analysis. A scalar hash in place of the matrix hash is a real
soundness bug we caught during development; we flag it because the
failure is silent and parameter-dependent.

\begin{lemma}[Wide-hash universality]
\label{lem:hash}
For keys $r_0, r_1, s, t \sample \Ftag$ define
$H(x_0, x_1) = r_0 P_s(x_0) + r_1 P_t(x_0) + x_1$, where
$x_0 \in \Ftag^{L}$ is parsed as polynomial coefficients,
$P_s(x_0) = \sum_{i} x_0[i]\, s^{\,L-1-i}$, and $x_1 \in \Ftag$. For
every $\delta_0 \ne 0$ and every $c \in \Ftag$,
\[
\Pr\bigl[\, r_0 P_s(\delta_0) + r_1 P_t(\delta_0) = c \,\bigr]
\;\le\; 2^{-\lambda} + \Bigl(\tfrac{L-1}{2^{\lambda}}\Bigr)^{2},
\]
so $H$ is $\varepsilon$-almost-universal in its $x_0$ argument with this
$\varepsilon$; and for uniform $x_1$ the output is uniform, so the pad
block makes the published hash perfectly hiding.
\end{lemma}

\begin{proof}
$P_s(\delta_0)$ is the evaluation at $s$ of a fixed nonzero polynomial of
degree at most $L-1$, so $\Pr_s[P_s(\delta_0) = 0] \le (L-1)/2^{\lambda}$,
and independently for $t$. Conditioned on
$(A, B) = (P_s(\delta_0), P_t(\delta_0)) \ne (0,0)$, the value
$r_0 A + r_1 B$ is uniform over the choice of the weight multiplying a
nonzero coordinate, so it hits $c$ with probability $2^{-\lambda}$.
Adding the probability of the event $(A,B) = (0,0)$ --- in which the
left side is $0$ and the bound must absorb $c = 0$ --- gives the claim.
\end{proof}

\subsection{MAYO}
\label{sec:mayo}

MAYO~\cite{Beullens21,MAYOspec} is an oil-and-vinegar~\cite{UOV} trapdoor
map: a public quadratic map $P : \Fsixteen^{n} \to \Fsixteen^{m}$
vanishing on a secret $o$-dimensional oil space, with $o < m$ deliberately
too small for direct Kipnis--Shamir-style attacks; the ``whipping''
construction evaluates $k_w$ correlated copies to stretch the oil space, so
the effective (whipped) public map is
$P^\ast : \Fsixteen^{k_w n} \to \Fsixteen^{m}$, matching the notation of the
MAYO literature. The trapdoor samples preimages
$\sigma$ with $P^\ast(\sigma) = y$ for arbitrary targets $y$; this
\emph{hash-then-invert} usage (rather than MAYO's message-signing API) is
exactly what the scheme needs. We use the round-2 parameter set
MAYO$_2$: $(n,m,o,k_w) = (81,64,17,4)$, so $\sigma \in \Fsixteen^{324}$
(162 bytes) and targets are $m = 64$ nibbles (32 bytes). We note the risk
posture explicitly: multivariate trapdoors have a difficult history ---
Rainbow fell to a practical attack in 2022~\cite{Beullens22} --- and MAYO's
whipping is comparatively young. The protocol also uses a one-hash,
signer-salted wrapper rather than MAYO's byte-for-byte message-signing API;
Remark~\ref{rem:salted} records the resulting reduction boundary. Exact spend
retries replay one durable response, while repeated authorized issuance
samples a new salt and hence a new target except on a charged collision. The
design compartmentalizes this risk:
a MAYO break forges credentials but is not expected to deanonymize past
transactions, whose privacy rests only on the hash-based layer
(Claim~\ref{claim:anon}).

For the proof circuit, the whipped map is expanded once per public key into
$m$ upper-triangular forms $G_a \in \Fsixteen^{k_w n \times k_w n}$ with
$P^\ast(\sigma)_a = \sigma^{\!\top} G_a\, \sigma$; the circuit then proves
$m$ quadratic equations over the \emph{same} $\binom{k_w n + 1}{2}$ product
terms (Section~\ref{sec:quadsys}). The $G_a$ table is derived locally and
never transmitted: at $m \binom{k_w n + 1}{2}$ nibbles --- about
1.7\,MB for MAYO$_2$, sixteen times the wire-format public key of
Table~\ref{tab:sizes} --- it is a real cost for constrained verifiers
(open problem~2 in Section~\ref{sec:open}).

\section{Definitions and assumptions}
\label{sec:definitions}

We state the syntax and the security games the eventual analysis must
target; a fully formal treatment of concurrent sessions and adaptive
corruptions is left to the reduction effort. Throughout the experiments:
the application label $\mathsf{app}$ and the parameter, profile, and
balance-width identifiers are fixed public constants; $\mathsf{KGen}$ is
MAYO trapdoor generation; $\mathsf{CanonParse}$ is the scheme's canonical
public-key decoder, returning $\bot$ on any non-canonical encoding; and
$\mathsf{DeriveCtx}(\mathsf{pk})$ is $\mathsf{Setup}$'s context derivation
applied to those constants and $\mathsf{pk}$. The maps $\Cred$,
$\mathsf{SignedTarget}$,
$\mathsf{digest}$, and $\mathsf{DeriveCtx}$ are all defined over one
shared prefix-consistent ideal XOF $\mathcal{H}$
(Remark~\ref{rem:xof}), which every experiment implicitly samples and
provides to all parties, including the adversary.

\begin{definition}[Anonymous credit token scheme]
\label{def:syntax}
An \emph{anonymous credit token scheme} with balance space
$\{0,\dots,2^{64}{-}1\}$ is a tuple of interactive algorithms
\[
(\mathsf{Setup},\ \mathsf{Issue},\ \mathsf{Spend},\
\mathsf{SpendDeferred},\ \mathsf{VerifyToken})
\]
between an issuer and clients:
\begin{itemize}
  \item $\mathsf{Setup}(1^\lambda, \mathsf{app}) \to
  (\mathsf{sk}, \mathsf{pk}, \ctx)$: generates the issuer trapdoor, public
  key, and a context binding the key, application domain, parameter set,
  and protocol version.
  \item $\mathsf{Issue}\langle \mathrm{C}(\mathsf{pk}, b),
  \mathrm{I}(\mathsf{sk}, b) \rangle \to T$ (public balance $b$): the client
  obtains a \emph{direct token} $T$ of effective balance $b$, or aborts.
  \item $\mathsf{Spend}\langle \mathrm{C}(\mathsf{pk}, T, s),
  \mathrm{I}(\mathsf{sk}) \rangle \to T'$ (public amount $s$): consumes
  $T$'s nullifier and yields a direct token $T'$ of effective balance
  $\mathsf{bal}(T) - s$, or aborts.
  \item $\mathsf{SpendDeferred}\langle \mathrm{C}(\mathsf{pk}, T, s),
  \mathrm{I}(\mathsf{sk}) \rangle \to T'$: as $\mathsf{Spend}$, except
  the issuer selects a return $t' \le s$ (by its return policy, a session
  input) after the client fixes its proved request and before the signer salt;
  the output is a
  \emph{deferred-return token} of effective balance
  $\mathsf{bal}(T) - s + t'$.
  \item $\mathsf{VerifyToken}(\mathsf{pk}, T) \to \{0,1\}$: local client
  authentication of a decoded token. Tokens are never sent as
  presentations; only spend transcripts are.
\end{itemize}
Both spend operations accept either token format as input; the four
input/output transitions carry distinct type tags bound into their
transcripts.
\end{definition}

\begin{definition}[Correctness, informal]
\label{def:correctness}
Consider an experiment in which an environment drives an honest issuer and
honest clients to build a DAG of tokens: roots arise from
$\mathsf{Issue}(b)$ and every internal node from spending a
not-yet-spent node, with amounts respecting balances. The scheme is correct
if, except with negligible probability, no honest operation aborts, every
client-side authentication check passes, every output balance obeys the
equations of Definition~\ref{def:syntax}, distinct accepted inputs yield
distinct nullifiers (colliding only with probability about
$q^{2}/2^{257}$ over $q$ accepted credentials, by the birthday bound on
the 256-bit suffix), and exact retransmission of a \emph{spend} request
returns the stored response rather than a new node (issuance carries no
retry cache: an accepted re-issuance resamples the preimage, but the
opening and nullifier are unchanged, so no new lineage arises).
\end{definition}

\begin{definition}[Fiscal soundness]
\label{def:fiscal}
For a scheme $\Pi$ and adversary $\adv$, the experiment
$\mathrm{Exp}^{\mathrm{fiscal}}_{\Pi,\adv}(\lambda)$ of
Figure~\ref{fig:fiscal} runs $\adv$ as all clients against an honest
issuer. $\Pi$ is \emph{fiscally sound} if for every efficient $\adv$,
$\mathrm{Adv}^{\mathrm{fiscal}}_{\Pi,\adv}(\lambda) :=
\Pr[\mathrm{Exp}^{\mathrm{fiscal}}_{\Pi,\adv}(\lambda) = 1]$
is negligible.
\end{definition}

\begin{figure}[H]
\centering
\begin{pcvstack}[boxed]
\procedure{$\mathrm{Exp}^{\mathrm{fiscal}}_{\Pi,\adv}(\lambda)$}{%
 (\mathsf{sk}, \mathsf{pk}, \ctx) \sample \mathsf{Setup}(1^\lambda,
   \mathsf{app}) \\
 I \gets 0; \quad R \gets 0; \quad \mathsf{St} \gets \emptyset; \quad
   \mathsf{win} \gets 0 \\
 \text{run } \adv^{\mathsf{oIssue}, \mathsf{oSpend},
   \mathsf{oSpendDef}}(\mathsf{pk}, \ctx) \\
 \pcreturn \mathsf{win}}
\pcvspace
\procedure{$\mathsf{oIssue}(b)$}{%
 \text{run the issuer side of } \mathsf{Issue}
   \text{ at balance } b \text{ with } \adv \text{ as client} \\
 \pcif \text{the issuer accepts:} \quad
   I \gets I + b; \; \pcreturn \text{its response} \\
 \pcreturn \bot}
\pcvspace
\procedure{$\mathsf{oSpend}(\mathit{req})$}{%
 \pcif \mathit{req} \text{ does not parse as a typed ordinary-settlement
   request:} \pcreturn \bot \\
 (\Null, s) \gets \text{its nullifier and amount}; \quad
   \mathit{dig} \gets \mathsf{digest}(\ctx, \mathit{req}) \\
 \pcif \mathsf{St}[\Null] = (\mathit{dig}', \mathit{resp}')
   \text{ exists:} \\
 \pcind \pcif \mathit{dig}' = \mathit{dig}: \pcreturn \mathit{resp}'
   \quad \pccomment{exact retry: no accounting change} \\
 \pcind \pcreturn \bot
   \quad \pccomment{conflicting reuse of a consumed nullifier} \\
 \pcif \text{issuer verification of } \mathit{req} \text{ fails:}
   \pcreturn \bot \\
 \mathit{resp} \gets \text{issuer response (fresh preimage on the fresh
   target)} \\
 \mathsf{St}[\Null] \gets (\mathit{dig}, \mathit{resp}); \quad
   R \gets R + s \\
 \pcif R > I: \mathsf{win} \gets 1 \\
 \pcreturn \mathit{resp}}
\pcvspace
\procedure{$\mathsf{oSpendDef}(\mathit{req}, t')$}{%
 \pcif \mathit{req} \text{ does not parse as a typed deferred-settlement
   request:} \pcreturn \bot \\
 (\Null, s) \gets \text{its nullifier and amount}; \quad
   \mathit{dig} \gets \mathsf{digest}(\ctx, \mathit{req}) \\
 \pcif \mathsf{St}[\Null] = (\mathit{dig}', \mathit{resp}')
   \text{ exists:} \\
 \pcind \pcif \mathit{dig}' = \mathit{dig}: \pcreturn \mathit{resp}'
   \quad \pccomment{exact retry replays the stored $t'$; accounting
   unchanged} \\
 \pcind \pcreturn \bot \\
 \pcif \text{issuer verification of } \mathit{req} \text{ fails}
   \;\vee\; \neg(0 \le t' \le s): \pcreturn \bot \\
 \mathit{resp} \gets \text{issuer response on the deferred target,
   recording } t' \\
 \mathsf{St}[\Null] \gets (\mathit{dig}, \mathit{resp}); \quad
   R \gets R + s - t' \\
 \pcif R > I: \mathsf{win} \gets 1 \\
 \pcreturn \mathit{resp}}
\end{pcvstack}
\caption{The fiscal-soundness experiment. Accounting is per logical spend
event (first durable consumption of a nullifier); letting $\adv$ choose
$t' \le s$ models issuer return policy conservatively. External redemption
of value must be idempotent at the same store boundary; symmetrically,
since $I$ counts every independently authorized issuance call and issuance
carries no retry cache, a deployment must replay ambiguous delivery under the
same external idempotency key rather than re-sign or re-charge --- issuance
idempotency lives at the payment boundary.}
\label{fig:fiscal}
\end{figure}

\begin{remark}[Redemption-only is without loss of generality]
The win condition counts only \emph{redeemed} value, not outstanding
spendable balances, so an attack that merely \emph{forks} a token into two
live successors has not yet won. It extends to a win at will: any
adversary holding more spendable value than the invariant allows simply
continues querying $\mathsf{oSpend}$ to cash the excess out, and the game
runs for the whole experiment. Formally, redemption-only soundness is
equivalent to the ledger invariant of the design document
(redeemed value $+$ live independently spendable value $\le$ issued value)
up to this cash-out reduction, which is the statement a full analysis
should prove as a lemma rather than assume.
\end{remark}

\begin{definition}[Spend anonymity, single challenge]
\label{def:anon}
For a scheme $\Pi$ and adversary $\adv$ (a malicious issuer), the
experiment $\mathrm{Exp}^{\mathrm{anon}}_{\Pi,\adv}(\lambda)$ of
Figure~\ref{fig:anon} is defined with the convention that the experiment's
explicit abort steps (an invalid public key or an invalid challenge
selection) output a uniformly random bit, so degenerate strategies gain
nothing; a malformed final guess is likewise replaced by a fresh coin.
Aborts the adversary induces \emph{inside} protocol sessions are not
experiment aborts. All handle state ($\mathsf{spent}$, $\mathsf{retired}$,
$\mathsf{exposed}$) initializes to 0, and $\mathsf{exposed}[h]$ is set as
soon as a spend \emph{request} on $h$ is delivered to $\adv$, whether or
not the session completes: a spend request reveals the token's
deterministic nullifier, so a candidate whose nullifier $\adv$ has already
seen would be trivially distinguishable.
$\Pi$ is \emph{spend-anonymous} if for every efficient $\adv$,
$\mathrm{Adv}^{\mathrm{anon}}_{\Pi,\adv}(\lambda) :=
\bigl|\Pr[\mathrm{Exp}^{\mathrm{anon}}_{\Pi,\adv}(\lambda) = 1] -
\tfrac{1}{2}\bigr|$ is negligible.
\end{definition}

\begin{figure}[H]
\centering
\begin{pcvstack}[boxed]
\procedure{$\mathrm{Exp}^{\mathrm{anon}}_{\Pi,\adv}(\lambda)$}{%
 \beta \sample \{0,1\}; \quad \mathcal{T} \gets \emptyset
   \quad \pccomment{handle table of honest tokens} \\
 \mathsf{pk}_{\mathrm{bytes}} \sample \adv(1^\lambda) \\
 \mathsf{pk} \gets \mathsf{CanonParse}(\mathsf{pk}_{\mathrm{bytes}});
   \quad \pcif \mathsf{pk} = \bot: \text{abort} \\
 \ctx \gets \mathsf{DeriveCtx}(\mathsf{pk})
   \quad \pccomment{as in $\mathsf{Setup}$; fixed hereafter} \\
 (h_0, h_1, s, \mathsf{mode}) \sample \adv^{\mathsf{oClientOp}}() \\
 \pcif \neg\mathsf{ValidChallenge}(h_0, h_1, s, \mathsf{mode}):
   \text{abort} \\
 \mathsf{retire}(h_0); \; \mathsf{retire}(h_1)
   \quad \pccomment{retired before the challenge session, so no oracle
   can touch them concurrently} \\
 \text{run the honest client side of the mode-}\mathsf{mode}
   \text{ spend of amount } s \text{ on } \mathcal{T}[h_\beta], \\
 \pcind \text{with } \adv \text{ as issuer; } \adv \text{ sees the
   transcript, and may abort the session} \\
 \pcind \pccomment{an issuer-induced challenge abort stays in the game} \\
 \pcif \text{the session accepts:} \quad
   \mathsf{spent}[h_\beta] \gets 1; \text{ store the successor at a
   fresh handle } h^\ast; \; \mathsf{retire}(h^\ast) \\
 \beta' \sample \adv^{\mathsf{oClientOp}}();
   \quad \pcif \beta' \notin \{0,1\}: \beta' \sample \{0,1\} \\
 \pcreturn [\beta' = \beta]}
\pcvspace
\procedure{$\mathsf{oClientOp}(\mathsf{op}, h, \mathit{args})$}{%
 \pcif \mathsf{op} \notin
   \{\mathsf{Issue}, \mathsf{Spend}, \mathsf{SpendDeferred}\}:
   \pcreturn \bot \\
 \pcif \mathsf{op} \ne \mathsf{Issue} \wedge
   (h \notin \mathcal{T} \vee \mathsf{retired}[h] \vee
   \mathsf{spent}[h]): \pcreturn \bot \\
 \text{run the honest client side of } \mathsf{op}
   \text{ on } \mathcal{T}[h] \text{ where required, with } \adv
   \text{ as issuer,} \\
 \pcind \text{performing every client-side authentication check;} \\
 \pcind \pcif \mathsf{op} \ne \mathsf{Issue}: \text{ set }
   \mathsf{exposed}[h] \gets 1 \text{ when the spend request is
   delivered to } \adv \\
 \pcif \text{the session accepts:} \\
 \pcind \pcif \mathsf{op} \ne \mathsf{Issue}:
   \mathsf{spent}[h] \gets 1 \\
 \pcind \text{store the output token at a fresh handle } h';
   \; \pcreturn h' \\
 \pcreturn \bot \quad \pccomment{aborted sessions yield no token, but
   exposure persists}}
\pcvspace
\procedure{$\mathsf{ValidChallenge}(h_0, h_1, s, \mathsf{mode})$}{%
 \pcreturn \mathsf{mode} \in \{\mathsf{Spend},
   \mathsf{SpendDeferred}\} \;\wedge\; h_0 \ne h_1 \\
 \pcind \wedge\; \mathcal{T}[h_0], \mathcal{T}[h_1]
   \text{ exist, } \neg\mathsf{retired}, \neg\mathsf{exposed},
   \text{ authenticated} \quad \pccomment{$\mathsf{spent} \Rightarrow
   \mathsf{exposed}$} \\
 \pcind \wedge\; \text{same credential kind} \;\wedge\;
   \mathsf{bal}(\mathcal{T}[h_0]) = \mathsf{bal}(\mathcal{T}[h_1]) \\
 \pcind \wedge\; s \text{ valid for both} \quad \pccomment{implied by
   equal balances; stated for clarity}}
\end{pcvstack}
\caption{The single-challenge spend-anonymity experiment against a
malicious issuer. The explicit ``abort'' steps (invalid key, invalid
challenge selection) output a uniformly random bit; an issuer-induced
abort of the challenge \emph{session} is ordinary protocol behavior ---
the transcript stands and $\adv$ still guesses, so selective-failure
strategies remain in scope. Handle-state conventions (initialization and
the exposure trigger) are part of Definition~\ref{def:anon};
``authenticated'' in $\mathsf{ValidChallenge}$ is implied by
storage-on-accept and stated for emphasis.}
\label{fig:anon}
\end{figure}

\begin{remark}[Design of the anonymity game]
\label{rem:anon}
The same-kind, same-mode, same-amount, equal-balance restrictions reflect
leakage the syntax makes public by design. Retiring the \emph{candidates}
closes the immediate distinguisher: replaying a candidate would reveal
$\beta$ through nullifier reuse (and requiring unexposed candidates
extends this to nullifiers revealed by \emph{aborted} pre-challenge
sessions). Retiring the \emph{successor} isolates the definition to a
single challenged transcript rather than smuggling in a compositional
guarantee about continued use. This is scope hygiene, not a defense
against a known distinguisher: with equal candidate balances the
successor's balance $\mathsf{bal} - s + t'$ is a deterministic function
of values $\adv$ already holds --- it chose $t'$ itself, even if as a
function of the observed nullifier --- so we know of no attack through a
live successor, and a proof that none exists is exactly the compositional
statement the stronger definition below would supply. A chain definition
(hidden, permuted token handles with identical admissible continuations)
is the natural strengthening and is left open. Section~\ref{sec:security} discusses the metadata any
deployment leaks outside this game.
\end{remark}

\begin{assumption}[Ordinary MAYO foundations]
\label{ass:mayo}
The OV problem and Multi-Target Whipped MQ problem of the MAYO Round-2
specification are hard at the selected parameters.  This is what we mean
below by ``ordinary MAYO hardness'': OV hides the structured oil subspace,
while Multi-Target Whipped MQ asks for a preimage of one of many random
targets under a random whipped map.  The latter is multi-target but gives
the adversary no trapdoor-inversion oracle, and therefore is strictly
different from the one-more game in Figure~\ref{fig:omp}.
\end{assumption}

\begin{figure}[H]
\centering
\begin{pcvstack}[boxed]
\procedure{$\mathrm{Exp}^{\mathrm{omp}}_{\adv}(\lambda)$}{%
 (\mathsf{sk}, \mathsf{pk}) \sample \mathsf{KGen}(1^\lambda); \quad
   \ctx \gets \mathsf{DeriveCtx}(\mathsf{pk}); \quad
   \mathcal{D} \gets \emptyset \\
 \{(y_j, w_j, \sigma_j)\}_{j=1}^{k} \sample
   \adv^{\mathcal{H}, \mathsf{oInv}}(\mathsf{pk}) \\
 \pcreturn 1 \text{ iff } k \ge |\mathcal{D}| + 1
   \;\wedge\; y_1, \dots, y_k \text{ pairwise distinct} \\
 \pcind \wedge\; \forall j: \mathsf{Adm}(y_j, w_j) = 1
   \;\wedge\; P^\ast(\sigma_j) = y_j}
\pcvspace
\begin{pchstack}
\procedure{$\mathsf{oInv}(y, w)$}{%
 \pcif \mathsf{Adm}(y, w) = 0: \pcreturn \bot \\
 \mathcal{D} \gets \mathcal{D} \cup \{y\} \\
 \pcreturn \Spre(\mathsf{sk}, y) \\
 \pccomment{fresh randomness per call}}
\pchspace
\procedure{$\mathsf{Adm}(y, w)$}{%
 \pcif y \notin \Fsixteen^{m}: \pcreturn 0 \\
 \pccomment{below: $\kappa, \rho \in \{0,1\}^{256}$,
   $b, t \in \{0, \dots, 2^{64}{-}1\}$} \\
 \pcif w = (\kappa, b, \rho) \wedge y = \Cred(\kappa, b, \rho):
   \pcreturn 1 \\
 \pcif w = (\kappa, b, \rho, t) \\
 \pcind \wedge\; y = \Def(\ctx, \Cred(\kappa, b, \rho), t):
   \pcreturn 1 \\
 \pcreturn 0}
\end{pchstack}
\end{pcvstack}
\caption{The adaptive one-more-preimage experiment required by the prior
unsalted direct-target design, shown for contrast and \emph{not} assumed by
the construction below.  The oracle power absent from ordinary MAYO is the
ability to request trapdoor preimages of adversarially selected admissible
targets.}
\label{fig:omp}
\end{figure}

\begin{remark}[Why the one-more game is stronger]
\label{rem:omp}
The prior design passed a client-derived target directly to $\Spre$.  A
simulator that chose a public preimage $\sigma$ could evaluate
$P^\ast(\sigma)$, but could not force that image to equal the already fixed
client target; Figure~\ref{fig:omp}'s inversion oracle supplied exactly the
missing power.  Merely proving that the client evaluated Keccak does not
change this direction of simulation.  PoMFRIT's optimized construction uses
an $(n,q)$ one-more assumption for this reason.  Its more general
hash-and-prove construction instead relies on ordinary MAYO security.
\end{remark}

\begin{remark}[Signer-salted wrapper and proof boundary]
\label{rem:salted}
The construction removes the problematic client-fixed-target interface and
same-target multiplicity constructively: the issuer signs only salted wrappers
\[
y \;=\; \mathcal{H}\bigl(\text{``signed''} \,\|\,
\mathsf{pack16}(\Cred) \,\|\, \enc(t) \,\|\, \zeta\bigr)[0:4m],
\qquad \zeta \sample \{0,1\}^{256}
\]
freshly after each request is accepted (with $t=0$ for the direct format).
The token stores $\zeta$, and the spend relation proves the wrapper with
$\zeta$ hidden.  Except for salt prediction/collision, the XOF point is fresh,
so the ordinary MAYO simulation can choose $\sigma$, compute
$P^\ast(\sigma)$, and program that value at the salted point.  An unsigned
output can then feed the OV and Multi-Target Whipped MQ hops.  Exact spend
retries replay the durable $(\zeta,\sigma)$ pair and do not create another
signing sample.

This is the ordinary MAYO \emph{proof shape}, not a verbatim invocation of
Round-2 Theorem~1: the wrapper encoding and salt derivation differ from
Algorithm~7.  An adapted classical-ROM proof must couple trapdoor sampling
to public sampling, paying the published rank-rejection factor
$(1-Q_{\mathsf{cpl}} B)^{-1}$ when $Q_{\mathsf{cpl}} B<1$, plus salt-prequery, collision, and local
sampler-failure terms.  For MAYO$_2$, $B\approx2^{-20}$, so even the
published proof has a per-key query boundary near $2^{19}$ signatures for a
constant-factor loss.  The 256-attempt implementation cap needs a distinct
argument: Lemma~1 averages the rank-failure probability over key generation
and a fresh vinegar sample, giving $\mathbb{E}[p_{\mathsf{key}}]\le B$, not a
uniform per-key $p_{\mathsf{key}}\le B$.  It therefore does not by itself
justify a $B^{256}$ cap-failure term for one reused key.  A stronger tail or
key-validation argument remains open. If $Q_{\mathsf{try}}$ is a hard
experiment-level maximum on sampler invocations (each containing at most 256
rank attempts), the cited lemma alone gives only the conservative cap term
$Q_{\mathsf{try}}B$. The coupling uses $Q_{\mathsf{cpl}}=Q_S$ only under
response-oblivious race linearization and confidentiality of losing
candidates. Merely replacing $Q_S$ by $Q_{\mathsf{try}}$ does not simulate a
winner selected through variable sampler timing; without the scheduling
premise, a separate race-leakage lemma remains open. A QROM treatment also
remains open. Omitting $\ctx$
from the outer encoding saves a rate block; $\Cred$ already commits it, and
one MAYO key is confined to one derived context.
\end{remark}

\begin{remark}[Hash modeling and adversary class]
\label{rem:xof}
The analysis models $\shake$ as an ideal XOF --- a \emph{prefix-consistent}
variable-output random oracle, so that reading $4m{+}256$ bits and reading
$4m$ bits of the same stream agree on their common prefix (ordinary
length-indexed random-oracle outputs do not automatically provide this) ---
for three distinct roles: the credential stream (hiding, collision and
preimage structure, suffix pseudorandomness given a prefix), the
GGM/vector-commitment PRG layer, and Fiat--Shamir. FIPS~202 standardizes
SHAKE but proves none of these properties; sponge indifferentiability is
the standard heuristic bridge. The claims of
Section~\ref{sec:security} target \emph{classical} PPT adversaries in this
ROM; the primitives are post-quantum candidates, but a QROM treatment
(quantum XOF access, quantum rewinding for extraction) is explicitly open,
so ``post-quantum'' describes the assumption class, not a proved quantum
security level.
\end{remark}

\FloatBarrier

\section{The VOLE-ACT construction}
\label{sec:scheme}

\subsection{Credentials and nullifiers from one XOF stream}

Fix a 256-bit context $\ctx$ binding the issuer key (by hash of the expanded
public map), application domain, parameter set, VOLE profile, and protocol
version. The client samples $\kappa, \rho \gets \{0,1\}^{256}$ and, for base
balance $b \in \{0,\dots,2^{64}{-}1\}$, derives the stream
\[
X = \shake(\text{``credential''} \,\|\, \ctx \,\|\, \kappa \,\|\, \enc(b) \,\|\, \rho),
\quad
\Cred = X[0 : 4m], \quad \Null = X[4m : 4m{+}256].
\]
For MAYO$_2$, $\Cred$ is $4m = 256$ bits; its width as a collision domain
matters for fiscal security (Section~\ref{sec:security}).
Both values come from a \emph{single} sponge evaluation --- the message fits
one Keccak rate block --- so proving both costs one in-circuit permutation.
The commitment prefix $\Cred$ is revealed to the issuer exactly once (at
signing time); the nullifier suffix $\Null$ is revealed exactly once (at
spending time). Under the ideal-XOF modeling, the suffix is pseudorandom
given the prefix, which is what divorces the spend from the issuance.

\subsection{One authenticated format, two typed settlement states}

Every token is $(\sigma,\kappa,b,\rho,t,\zeta)$, where $\zeta$ is a
uniform 256-bit signer salt and
\[
\begin{aligned}
P^\ast(\sigma) &\;=\;
\mathsf{SignedTarget}(\Cred(\kappa,b,\rho),t,\zeta), \\
\mathsf{SignedTarget}(\Cred,t,\zeta)
&\;=\; \shake\bigl(\text{``signed''} \,\|\,
\mathsf{pack16}(\Cred) \,\|\, \enc(t) \,\|\, \zeta\bigr)[0:4m].
\end{aligned}
\]
Its effective balance is $b+t$ (exact, non-wrapping).  A \emph{direct}
token constrains $t=0$; a \emph{deferred-return} token carries an
issuer-chosen $t$. (The formal tuple elides fixed metadata the implementation
stores and $\mathsf{VerifyToken}$ checks: the derived context and
local credential-kind marker.) At $t=0$ the two markers intentionally name the
same semantic credential: retagging that token does not create a new
nullifier lineage or effective balance. The common hash gives a simple binding of the base
commitment, return, and signer salt:
changing $t$ requires a fresh preimage or a target collision, and no
algebraic combiner (such as $\Cred + \iota(t)$) has to be analyzed for
malleability at all.

Spends are typed. An ordinary $\mathsf{Spend}$ consumes either format and
always outputs a direct token; $\mathsf{SpendDeferred}$ consumes
either format and outputs a deferred-return token. The client commits to a
fresh $(\kappa', \rho', b')$ \emph{before} the issuer chooses $t' \le s$, which is
precisely the case that cannot be expressed as a smaller net spend --- if the
return were known in advance, the client would simply spend less. Requests,
proof statements, pending states, settlement responses, and retry records
carry distinct mode tags, but the wire header is not itself an authenticator:
retagged identical-layout bodies can parse. Proof statements and request
digests supply end-to-end mode binding. Zero-return token and response aliases
are deliberate and fiscally inert. A spend of
amount $s = 0$ is deliberately admissible: it consumes the old nullifier
and rotates $(\kappa, \rho)$, an unlinkable \emph{refresh} of an
unchanged balance --- useful, for example, after suspected exposure of
client state. (Each refresh writes a durable nullifier record at zero
redeemed value, so deployments should rate-limit or price it.)

\subsection{Proof relations}
\label{sec:relation}

The construction proves membership in three NP relations. Define the
helper maps (over the shared ideal XOF $\mathcal{H}$; $\mathsf{pack16}$
packs an $\Fsixteen$-vector two nibbles per byte, first element in the
low nibble):
\begin{figure}[H]
\centering
\small
\begin{pcvstack}[boxed]
\begin{pchstack}
\procedure{$\mathsf{CredStream}_{\ctx}(\kappa, b, \rho)$}{%
 X \gets \mathcal{H}(\text{``credential''} \,\|\, \ctx \,\|\, \kappa
   \,\|\, \enc(b) \,\|\, \rho) \\
 \Cred \gets X[0{:}4m] \text{ as } \Fsixteen^{m} \\
 \Null \gets X[4m{:}4m{+}256] \\
 \pcreturn (\Cred, \Null)}
\pchspace
\procedure{$\mathsf{Target}(T)$}{%
 (\Cred, \_) \gets \mathsf{CredStream}_{\ctx_T}(\kappa_T, b_T, \rho_T) \\
 \pcreturn \mathsf{SignedTarget}(\Cred, t_T, \zeta_T)}
\end{pchstack}
\pcvspace
\procedure{$\mathsf{SignedTarget}(\Cred, t, \zeta)$}{%
 \pcreturn \mathcal{H}(\text{``signed''} \,\|\,
   \mathsf{pack16}(\Cred) \,\|\, \enc(t) \,\|\, \zeta)[0{:}4m]}
\pcvspace
\procedure{$\mathsf{VerifyToken}(\mathsf{pk}, T)$}{%
 \pcif \ctx_T \ne \mathsf{DeriveCtx}(\mathsf{pk}): \pcreturn 0 \\
 \pcif \mathsf{kind}(T) = \mathsf{Direct} \wedge t_T \ne 0: \pcreturn 0 \\
 \pcif b_T + t_T \ge 2^{64}: \pcreturn 0 \\
 \pcreturn [P^\ast(\sigma_T) = \mathsf{Target}(T)]}
\end{pcvstack}
\caption{Helper maps and local token authentication. A token carries the
tuple $(\mathsf{kind}, \sigma, \kappa, b, \rho, t,\zeta)$, with $t = 0$ for the
direct format, together with its stored context $\ctx_T$; the effective
balance is $\mathsf{bal}(T) = b_T + t_T$. The maps are written
recomputing $\mathsf{CredStream}$ throughout; implementations cache the
derived commitment, but a token \emph{stores} none --- $\Cred$ is always
recomputed from the opening, so no stale commitment/opening mismatch can
arise.}
\label{fig:helpers}
\end{figure}

The issuance relation states that a revealed digest opens to the public
balance; the two spend relations state possession of a preimage for the
input token's target, correct nullifier derivation, exact balance
conservation, and well-formedness of the fresh commitment:
\begin{align*}
\mathcal{R}_{\mathsf{issue}} = \bigl\{\,
 &(\ctx, b, \Cred);\; (\kappa, \rho) \;:\;
 (\Cred, \cdot) = \mathsf{CredStream}_{\ctx}(\kappa, b, \rho)
 \,\bigr\} \\[0.4em]
\mathcal{R}_{\mathsf{spend}}^{\mathsf{Dir}} = \bigl\{\,
 &(\ctx, s, \Null, \Cred');\;
   (\sigma, \kappa, b, \rho, t,\zeta,e, \kappa', b', \rho') \;: \\
 &(\Cred, \Null) = \mathsf{CredStream}_{\ctx}(\kappa, b, \rho)
 \;\wedge\; t=0
 \;\wedge\; P^\ast(\sigma) = \mathsf{SignedTarget}(\Cred,t,\zeta) \\
 &\wedge\; b+t=e \;\wedge\; b' + s = e
   \text{ exactly (no 64-bit wrap)} \\
 &\wedge\; (\Cred', \cdot) =
   \mathsf{CredStream}_{\ctx}(\kappa', b', \rho')
 \\[-0.2em]
 &\,\bigr\} \\[0.4em]
\mathcal{R}_{\mathsf{spend}}^{\mathsf{Def}} = \bigl\{\,
 &(\ctx, s, \Null, \Cred');\;
   (\sigma, \kappa, b, \rho, t,\zeta,e, \kappa', b', \rho') \;: \\
 &(\Cred, \Null) = \mathsf{CredStream}_{\ctx}(\kappa, b, \rho)
 \;\wedge\; P^\ast(\sigma) = \mathsf{SignedTarget}(\Cred,t,\zeta) \\
 &\wedge\; b+t=e \;\wedge\; b' + s=e
   \text{ exactly (both sums below } 2^{64}) \\
 &\wedge\; (\Cred', \cdot) =
   \mathsf{CredStream}_{\ctx}(\kappa', b', \rho')
 \\[-0.2em]
 &\,\bigr\}
\end{align*}
The balance equations are exact 64-bit unsigned additions with zero final
carry, proven over committed carry bits: there is no modular wraparound to
exploit, and overspending is exactly the statement ``the final carry is
1,'' which the relation excludes. All witness scalars range over their
stated domains ($\kappa, \rho \in \{0,1\}^{256}$;
$b, b', t \in \{0, \dots, 2^{64}{-}1\}$).

\subsection{Protocol algorithms}
\label{sec:algorithms}

Figures~\ref{fig:issue-alg} and~\ref{fig:spend-alg} instantiate
Definition~\ref{def:syntax} against three black-box interfaces: the
trapdoor map ($P^\ast$, $\Spre$, $\mathsf{KGen}$), the shared XOF
$\mathcal{H}$, and a non-interactive zero-knowledge argument of knowledge
$\Pi = (\mathsf{Prove}, \mathsf{Verify})$ for the relations of
Section~\ref{sec:relation}, where the full typed statement --- including
$\ctx$ and the credential-kind and settlement-mode tags --- is bound into
the challenge derivation. Section~\ref{sec:circuit} instantiates $\Pi$ by
executing each relation as a Boolean circuit under the VOLEitH proof
system; an implementation of the protocol layer needs only these
interfaces. $\mathsf{digest}(\ctx, \mathit{req}) =
\mathcal{H}(\text{``request-digest''} \,\|\, \ctx \,\|\,
\mathsf{frame}(\mathit{req}))[0{:}256]$ is the exact-retry key.

\begin{figure}[H]
\centering
\begin{pcvstack}[boxed]
\procedure{$\mathsf{Setup}(1^\lambda, \mathsf{app})$}{%
 (\mathsf{sk}, \mathsf{pk}) \sample \mathsf{KGen}(1^\lambda)
   \quad \pccomment{MAYO trapdoor generation; expand whipped forms} \\
 h_{\mathsf{pk}} \gets \mathcal{H}(\text{``mayo-pk''} \,\|\,
   \text{canonical encoding of the expanded } P^\ast) \\
 \ctx \gets \mathcal{H}(\text{``context''} \,\|\,
   \mathsf{frame}(\mathsf{app}) \,\|\, h_{\mathsf{pk}} \,\|\,
   \text{parameter/profile identifiers} \,\|\,
   \text{balance width})[0{:}256] \\
 \pcreturn (\mathsf{sk}, \mathsf{pk}, \ctx)}
\pcvspace
\begin{pchstack}
\procedure{$\mathsf{Issue.Client}(\mathsf{pk}, \ctx, b)$}{%
 \kappa, \rho \sample \{0,1\}^{256} \\
 (\Cred, \_) \gets \mathsf{CredStream}_{\ctx}(\kappa, b, \rho) \\
 \pi \sample \Pi.\mathsf{Prove}(\mathcal{R}_{\mathsf{issue}},
   (\ctx, b, \Cred), (\kappa, \rho)) \\
 \pcreturn (\mathit{req} = (\Cred, \pi),\;
   \mathit{st} = (\kappa, b, \rho, \Cred))}
\pchspace
\procedure{$\mathsf{Issue.Issuer}(\mathsf{sk}, \ctx, b, (\Cred, \pi))$}{%
 \pccomment{$b$: externally authorized} \\
 \pccomment{session input, not in the request} \\
 \text{require } \Pi.\mathsf{Verify}(\mathcal{R}_{\mathsf{issue}},
   (\ctx, b, \Cred), \pi) \\
 \zeta \sample \{0,1\}^{256}; \quad
 Y \gets \mathsf{SignedTarget}(\Cred,0,\zeta); \quad
 \sigma \sample \Spre(\mathsf{sk},Y) \\
 \pcreturn (\zeta,\sigma)}
\end{pchstack}
\pcvspace
\procedure{$\mathsf{Issue.Finish}(\mathsf{pk}, \mathit{st}, (\zeta,\sigma))$}{%
 \text{require } P^\ast(\sigma) = \mathsf{SignedTarget}(\Cred,0,\zeta) \\
 \pcreturn T = (\mathsf{Direct}, \sigma, \kappa, b, \rho, t{=}0,\zeta)}
\end{pcvstack}
\caption{Setup and issuance.}
\label{fig:issue-alg}
\end{figure}

\begin{figure}[H]
\centering
\begin{pcvstack}[boxed]
\procedure{$\mathsf{Spend.Client}(\mathsf{pk}, \ctx, T, s, \mathsf{mode})$}{%
 \text{require } \mathsf{VerifyToken}(\mathsf{pk}, T) \wedge
   s \le \mathsf{bal}(T) \\
 \kappa', \rho' \sample \{0,1\}^{256}; \quad
   b' \gets \mathsf{bal}(T) - s \\
 (\Cred', \_) \gets \mathsf{CredStream}_{\ctx}(\kappa', b', \rho');
   \quad
 (\_, \Null) \gets \mathsf{CredStream}_{\ctx}(\kappa_T, b_T, \rho_T) \\
 x \gets (\ctx, \mathsf{kind}(T), \mathsf{mode}, s, \Null, \Cred') \\
 e_T \gets b_T+t_T; \quad
 w \gets (\sigma_T, \kappa_T, b_T, \rho_T, t_T,\zeta_T,e_T,
   \kappa', b', \rho') \\
 \pi \sample \Pi.\mathsf{Prove}(
   \mathcal{R}_{\mathsf{spend}}^{\mathsf{kind}(T)}, x, w) \\
 \pcreturn (\mathit{req} = (\mathsf{kind}(T), \mathsf{mode}, s, \Null,
   \Cred', \pi),\; \mathit{st} = (\kappa', b', \rho', \Cred', s,
   \mathsf{mode}))}
\pcvspace
\procedure{$\mathsf{Spend.Issuer}(\mathsf{sk}, \ctx, \mathsf{St},
  \mathit{req}, \mathsf{policy})$}{%
 \mathit{dig} \gets \mathsf{digest}(\ctx, \mathit{req}) \\
 \pcif \mathsf{St}[\mathit{req}.\Null] = (\mathit{dig}', \mathit{resp}')
   \text{ exists:} \quad \pcreturn \mathit{resp}' \text{ if }
   \mathit{dig}' = \mathit{dig} \text{ else } \bot \\
 x \gets (\ctx, \mathit{req}.\mathsf{kind}, \mathit{req}.\mathsf{mode},
   \mathit{req}.s, \mathit{req}.\Null, \mathit{req}.\Cred') \\
 \pcif \mathit{req}.\mathsf{mode} = \mathsf{Spend}: \quad t' \gets 0 \\
 \pcelse: \quad t' \gets \mathsf{policy}(\mathit{req}) \text{ with }
   0 \le t' \le \mathit{req}.s \\
 \text{require } \Pi.\mathsf{Verify}(
   \mathcal{R}_{\mathsf{spend}}^{\mathit{req}.\mathsf{kind}}, x,
   \mathit{req}.\pi) \\
 \zeta' \sample \{0,1\}^{256}; \quad
 Y' \gets \mathsf{SignedTarget}(\mathit{req}.\Cred',t',\zeta'); \quad
 \sigma' \sample \Spre(\mathsf{sk},Y') \\
 \mathit{resp} \gets (t',\zeta',\sigma') \\
 \text{\emph{atomically} insert } \mathsf{St}[\mathit{req}.\Null] \gets
   (\mathit{dig}, \mathit{resp});
   \text{ let } (\mathit{dig}^\ast, \mathit{resp}^\ast)
   \text{ be the durable record} \\
 \pcreturn \mathit{resp}^\ast \text{ if } \mathit{dig}^\ast =
   \mathit{dig} \text{ else } \bot}
\pcvspace
\procedure{$\mathsf{Spend.Finish}(\mathsf{pk}, \mathit{st},
  \mathit{resp})$}{%
 (t',\zeta',\sigma') \gets \mathit{resp}; \quad
 \text{require } P^\ast(\sigma')=
   \mathsf{SignedTarget}(\Cred',t',\zeta') \\
 \pcif \mathit{st}.\mathsf{mode} = \mathsf{Spend}: \quad
   \text{require }t'=0; \quad
   \pcreturn (\mathsf{Direct}, \sigma', \kappa', b', \rho', 0,\zeta') \\
 \text{require } t' \le \mathit{st}.s; \quad
 \pcreturn (\mathsf{Deferred}, \sigma', \kappa', b', \rho', t',\zeta')}
\end{pcvstack}
\caption{Spending, both settlement modes. $\mathsf{policy}$ is the
issuer's return-selection function; the durable-record comparison
implements exact-retry replay and conflicting-reuse rejection under
concurrent insertion.}
\label{fig:spend-alg}
\end{figure}

The client-side requirement $\mathsf{VerifyToken}$ and the finish-time
checks are the complete authentication surface the anonymity game relies
on. An ordinary spend of a deferred-return token folds the old return
into $b'$ and emits the common format with $t'=0$; the issuer verifies,
signs, and durably stores the response under $\Null$, so exact
retransmissions replay and a network failure cannot burn a balance.

\section{The proof circuit}
\label{sec:circuit}

The relation above is almost entirely SHAKE256: every spend proves three
hidden sponge evaluations (old stream, signed wrapper, fresh stream).
Everything else ---
the MAYO equation and the balance arithmetic --- is quadratic. This section
describes the three techniques that make the circuit small.

\subsection{Degree-16 Keccak checkpointing}
\label{sec:checkpoint}

Keccak-$f[1600]$'s round function is degree 2 (the $\chi$ step is the only
nonlinearity)~\cite{FIPS202}. The naive circuit commits all 1600 state bits
after every round: 24 rounds $\times$ 1600 witness bits, with degree-2
constraints between layers. But witness bits are the dominant cost in a
VOLE-style proof (each consumes a VOLE coordinate, enlarging both the
correction data and every leaf-PRG expansion), while raising the
\emph{degree} of the batched QuickSilver check is cheap: a maximum degree
$d$ costs $d$ published field elements and $d-1$ mask VOLEs \emph{in total}.

We therefore commit the state only every \emph{four} rounds. Between
checkpoints the circuit carries symbolic polynomial expressions: after
rounds $1,2,3,4$ the expressions have degree $2,4,8,16$, and the checkpoint
constraint asserts (per bit) that the degree-16 expression equals the next
committed state --- a degree-16 polynomial constraint, the largest the
instantiation accepts. This quarters the per-permutation witness from
$24 \times 1600$ to $6 \times 1600 = 9600$ bits. One permutation dominates
each hidden SHAKE evaluation, so both spend witnesses are $31{,}760$ bits
for MAYO$_2$ --- versus roughly $118{,}200$ under per-round
checkpointing. The cost is
the coefficient-vector products of the expression layer, about
$1.14$ million $\Ftag$ multiplications per permutation --- and the
\emph{verifier} pays the same evaluation over its keys: measured on the
prototype, this expression layer is the dominant term of issuer
verification, ahead of the quadratic-system folds and the seed-tree
reconstruction (Section~\ref{sec:performance}).

\subsection{The quadratic-system constraint for MAYO}
\label{sec:quadsys}

Verifying $P^\ast(\sigma) = y$ means checking $m = 64$ quadratic equations
\[
\sum_{r \le c} G_a[r,c]\; \sigma_r \sigma_c \;=\; y_a,
\qquad a \in [m],
\]
over the \emph{same} $\binom{325}{2} = 52{,}650$ products
$\sigma_r \sigma_c$. Materializing each product as a committed wire would
multiply the witness; instead the proof system supports a \emph{deferred
quadratic system}: the prover records the values and tags of every term
factor, and at challenge time the $m$ equations are folded into a single
QuickSilver degree-2 check by random weights $\varphi_a \in \Ftag$ drawn
from the transcript, with coefficients lifted through the embedding $\iota$.
An unsatisfied equation survives the fold except with probability
$2^{-\lambda}$. The committed signature nibbles enter as bit wires
($4 k_w n = 1296$ bits). The same mechanism folds the 64 quadratic carry
equations of each balance addition.

\subsection{Balance arithmetic in \texorpdfstring{$\Ftwo$}{GF(2)}}

Additions are proven bitwise with committed carry vectors:
$a_i + b_i + c_i = s_i$ linearly (over $\Ftwo$, addition is XOR --- an early
design error we caught was writing balance equations additively over the tag
field, where they say nothing about integers), and the carry recurrence
$c_{i+1} = a_i b_i + c_i(a_i + b_i)$ quadratically via the folded system.
Asserting the final carry to be zero makes every balance equation an exact
statement about 64-bit unsigned integers, so conservation is enforced
bit-exactly rather than modulo anything.

\section{Security analysis}
\label{sec:security}

We state the intended results as conditional claims against the definitions
of Section~\ref{sec:definitions}; no complete reduction is claimed, and
where the implementation's own analysis states sharper caveats, those
govern.

\begin{claim}[Fiscal soundness, conditional]
\label{claim:fiscal}
In the ideal-XOF model of Remark~\ref{rem:xof}, under
Assumption~\ref{ass:mayo} and the signer-salted wrapper reduction sketched in
Remark~\ref{rem:salted}, adaptive multi-theorem \emph{straight-line}
knowledge soundness (online extractability from the random-oracle
transcript, without rewinding, at proof acceptance and before current-output
signing) of the VOLEitH argument for \emph{both}
the issuance and the spend relations, a linearizable, non-rollback nullifier
store with confidential race losers and response-oblivious winner selection,
and service-level atomic issuance authorization, charging, idempotent durable
recording, and first publication, VOLE-ACT satisfies
Definition~\ref{def:fiscal}.
\end{claim}

\noindent\emph{Discussion and error terms.}
Conditionally on extraction, every accepted operation yields an opening
and a MAYO preimage for an admissible target, correct nullifier
derivation, and exact balance arithmetic.  Nullifier-class accounting turns
over-redemption, outside collision and store failures, into an EUF-CMA
forgery for the common salted wrapper: an authenticated message descriptor
$(\Cred,t)$ not previously submitted to the wrapper signer. The reduction
halts on the first such extracted input before signing the current output, so
no current or later query can retroactively submit that message. Remark~\ref{rem:salted}
sketches how the ordinary MAYO simulation handles this forgery without a
one-more oracle. Two credential-stream openings with equal $\Cred$ are a bad
event in the fiscal-to-unsigned-message lift. A collision between an unsigned
wrapper input and a programmed signing target need not be a separate abort in
the later MTWMQ game: the transferred preimage itself solves that random
target. It still exposes the generic ceiling of the same $4m$-bit output
space, and a finished advantage statement must place the event explicitly.
A further operational collision event: the
retry store retains a 256-bit \emph{request digest} rather than the full
request, so exact-retry semantics additionally assume collision
resistance of that digest (or full-request comparison), and the redeemed
service itself --- not merely the token response --- must be tied
idempotently to the first durable record. Nullifier collisions, by
contrast, need no fiscal accounting: two distinct accepted credentials
with colliding $\Null$ only make the store refuse whichever spends
second --- a liveness failure charged to correctness
(Definition~\ref{def:correctness}), never extra value. Issuance soundness
matters because it ties every signed wrapper message to an extracted opening;
without it, a malicious issuance request could obtain a signature on an
unaccounted descriptor. The dominant known terms and generic ceilings
include (this is not yet a complete advantage expression --- proof-attempt
counts, $Q_H$ factors, vector-commitment and consistency terms, and
composition loss are unquantified):
\begin{itemize}
  \item The degree-16 batched QuickSilver check contributes up to
  $17/2^{128} \approx 2^{-123.9}$ per proof attempt, before
  vector-commitment, consistency-hash, Fiat--Shamir, and composition terms.
  \item The credential digest is a collision domain of only $4m = 256$
  bits. Two openings $(\kappa,b,\rho) \ne (\kappa',b',\rho')$ with the same
  prefix $\Cred$ let the \emph{one} issuer-supplied preimage $\sigma$ be
  presented under both --- their suffix nullifiers will generally differ ---
  double-spending, and with distinct balances creating value. This is
  generic collision search: about $2^{128}$ classically, and query
  complexity $\Theta(2^{256/3}) \approx 2^{85.3}$ for an ideal quantum
  collision adversary (upper bound from BHT~\cite{BHT}, under its
  demanding memory model; matching query lower bound from
  Aaronson--Shi~\cite{AaronsonShi}).
  Deployments requiring a larger quantum collision margin need a wider
  digest, hence a larger $m$. Any concrete birthday term must count all
  distinct credential-XOF inputs, including adversarial offline grinding as
  well as honest and extracted openings; it cannot count only the two inputs
  appearing in the final collision.
\end{itemize}
Honest double spends are prevented operationally: a repeated credential
reveals the same deterministic nullifier, whose conflicting reuse the
store refuses while exact retransmissions are replayed from the stored
record --- the atomic retry semantics that also make honest
retransmission safe.

\begin{claim}[Spend anonymity, conditional]
\label{claim:anon}
In the ideal-XOF model of Remark~\ref{rem:xof}, assuming the VOLEitH
argument is adaptive multi-theorem non-interactive zero knowledge for all
three chosen-key relations $\mathcal{R}_{\mathsf{issue}}$,
$\mathcal{R}_{\mathsf{spend}}^{\mathsf{Dir}}$, and
$\mathcal{R}_{\mathsf{spend}}^{\mathsf{Def}}$ (i.e., against maliciously
formed but canonically parsed public keys, in the ROM), VOLE-ACT satisfies
Definition~\ref{def:anon}; the claim does not depend on any MAYO
assumption.
\end{claim}

\noindent\emph{Discussion.}
A spend reveals $(s, \Null, \Cred', \text{proof})$. The intended argument:
the compiled argument is NIZK zero-knowledge for all three relations in
the sense above (the $2\lambda$-bit per-proof salt is multi-instance
\emph{hardening}, not by itself a theorem upgrading honest-verifier ZK to
this property) --- zero knowledge of the \emph{issuance} proof is as
load-bearing as that of the spend proofs, since it argues about the very
stream whose unrevealed suffix is the nullifier, and a leaky issuance
proof would link the later spend with no MAYO involvement; $\Cred'$
hides its opening under the fresh 256-bit nonce $\rho'$; and $\Null$ is
the unrevealed suffix of an XOF stream whose prefix the issuer saw at
issuance, pseudorandom given the prefix in the ideal-XOF model. None of
these steps is currently backed by a proof for this exact composition, so
the claim is conditional on all three. Its MAYO-independence is the design's compartmentalization: a
trapdoor recovery or forgery does not help the distinguisher in
Definition~\ref{def:anon}. Outside the game, deployments leak metadata the
definition deliberately excludes: spend amounts are public; direct-input
and deferred-input proofs now have the same circuit and byte size, but the
explicit credential-kind and settlement tags, timing, network identity, and
retry behavior remain visible. Two further channels
deserve explicit flags. The definition hands every session one
$\mathsf{pk}$, so key \emph{consistency} is assumed, not provided: a
malicious issuer serving different (individually canonical) keys to
different clients partitions the anonymity set completely, since each
spend verifies under exactly one derived $\ctx$ --- deployments need key
transparency or another consistency mechanism, as in Privacy-Pass-style
systems. And unlike the passive channels above, the deferred return is
\emph{active}: an issuer can watermark a client by choosing a distinctive
$t'$, which is exactly what the equal-balance restriction on challenge
candidates excludes from the game --- return policies should be
deterministic public functions of public data (say, of $s$ and the
credential kind).

\paragraph{Multi-instance hardening.}
Every PRG and commitment call in the tree layer binds the per-proof
salt, the per-tree derived salt, and the exact tree position
(after~\cite{OneTree}; Section~\ref{sec:voleith}), preventing the known
multi-target and multi-instance amortization attacks on the 128-bit
seeds; a concrete multi-user bound
still carries adversary-instance-dependent terms. The wide consistency hash
removes the $2^{-h}$ per-chunk cheating strategy. Domain tags separate every
hash use. Encodings are canonical within each requested typed decoder, which
rejects alternate encodings of that typed value. Identical-layout bodies can
be header-retagged into another local marker, however: wire tags are
discriminants rather than authenticators, while proof statements, request
digests, and semantic checks provide end-to-end binding.

\section{Parameters and evaluation}
\label{sec:performance}

The construction is implemented as a Rust research prototype (five crates:
binary fields, GGM vector commitments, the VOLEitH proof system, MAYO, and
the protocol). Table~\ref{tab:sizes} gives wire sizes; tokens are
profile-independent and only proof-carrying requests grow with the
low-latency profiles. On one Apple M4 Pro, the common spend relation proves
in 29--31\,ms across profiles. Criterion measured Balanced-profile issuer
verify-and-sign at 9.2--9.4\,ms for spends (issuance: 3.37\,ms); no
cross-profile issuer range is inferred from that run. Direct and deferred
inputs now differ only by zero constraints and have identical proof sizes.
Figures are statistically sampled central estimates on an idle machine. The
compact profile's proofs are $3.8\times$ smaller than low-latency.
Implementation techniques,
optimization methodology, and full per-profile measurements are documented
in the repository,
\url{https://github.com/SamuelSchlesinger/vole-act}
(\texttt{docs/IMPLEMENTATION.md}, \texttt{docs/BENCHMARKS.md}; the latter
includes the 2026-07-21 signer-salted experiment and a labeled pre-wrapper
baseline).

\begin{table}[h]
\centering
\caption{Wire sizes (bytes), MAYO$_2$, $\lambda = 128$. Spend responses
are uniformly framed --- the direct mode carries the deferred mode's
return field structurally zero. Responses and tokens additionally carry a
32-byte signer salt. The
public-key row is the transmitted expanded MAYO map; the locally derived
$G_a$ table of Section~\ref{sec:mayo} ($\approx$1.7\,MB) is never sent.}
\label{tab:sizes}
\begin{tabular}{lrrr}
\toprule
Object & compact & balanced & low-latency \\
& $(\tau,h){=}(16,8)$ & $(32,4)$ & $(64,2)$ \\
\midrule
Token (either format) & 315 & 315 & 315 \\
Issue request & 29\,750 & 55\,286 & 106\,358 \\
Spend request (either input) & 73\,086 & 141\,918 & 279\,582 \\
Issue / spend response & 203 / 211 & 203 / 211 & 203 / 211 \\
Public key (expanded map) & 106\,319 & 106\,319 & 106\,319 \\
\bottomrule
\end{tabular}
\end{table}

\section{Related work}

VOLE-based designated-verifier ZK begins with
Wolverine~\cite{Wolverine} and Mac'n'Cheese~\cite{MacNCheese};
QuickSilver~\cite{QuickSilver} gives the degree-$d$ batched check we
generalize. VOLE-in-the-Head~\cite{VOLEitH} removes the designated verifier
and underlies the FAEST signature candidate~\cite{FAEST}, whose engineering
(GGM trees, salting, consistency checking) we follow, with the hardening of
\cite{OneTree}. MAYO~\cite{Beullens21,MAYOspec} descends from unbalanced
oil-and-vinegar~\cite{UOV}; \cite{Beullens22} is the cautionary tale for the
assumption class. The closest post-quantum
relative is PoMFRIT~\cite{PoMFRIT}, a \emph{blind-signature} scheme that
likewise combines VOLE-in-the-Head proofs of SHA-3-family circuits with the
MAYO trapdoor. Its general construction proves the hash/signature relation
and uses ordinary MAYO security; its optimized direct-target construction
introduces the one-more-preimage assumption. This distinction motivates our
signer-salted wrapper. PoMFRIT pursues the blindness route with the same
toolbox, while VOLE-ACT's commit-then-sign design avoids
blindness and adds stateful balances, typed settlement, and deferred
returns. Two other post-quantum routes give contrast. Generic succinct
proof systems yield credentials by proving possession of a conventionally
signed credential (the zk-creds template~\cite{zkcreds}); instantiated
with hash-based, STARK-style proofs the route is plausibly post-quantum
and far more succinct than ours, but with a substantially heavier prover
on exactly the hash statements VOLEitH proves natively, and with no
analogue of the free $\Fsixteen \subseteq \Ftag$ treatment of the
trapdoor equation. Structured lattices give practical anonymous
credentials and round-optimal blind signatures~\cite{BLNS23,BLNSblind}
with compact transcripts under algebraic lattice assumptions; against
both routes, VOLE-ACT trades succinctness for a symmetric-heavy
assumption base, a fast transparent prover, and exact balance arithmetic,
at the price of 71\,KiB proofs and an online stateful issuer. Anonymous
tokens and credentials span
blind signatures~\cite{Chaum82}, CL signatures~\cite{CL01},
keyed-verification credentials from algebraic MACs~\cite{CMZ14}, and
deployed unit-token systems~\cite{PrivacyPass}; VOLE-ACT is a post-quantum
candidate stateful-balance member of this family, instantiating the
Katz--Schlesinger design~\cite{KS}.

\section{Open problems}
\label{sec:open}

(1)~A complete security reduction: formalizing
Definitions~\ref{def:fiscal}--\ref{def:anon} fully (concurrency, chains,
adaptive corruptions) and proving Claims~\ref{claim:fiscal}
and~\ref{claim:anon} in the QROM, including the exact salted-wrapper
adaptation of MAYO's OV/MTWMQ proof and its rejection-sampling loss.
(2)~A structured, non-materialized QuickSilver check for the whipped MAYO
equation, removing the $O(m (k_w n)^2)$ expanded-form table
($\approx$1.7\,MB for MAYO$_2$; Section~\ref{sec:mayo}) and enabling the
larger MAYO parameter sets (whose wider digests also raise the quantum
collision ceiling).
(3)~Hashing the compact public-key encoding (rather than the expanded forms)
into the context --- a transcript-breaking cleanup best done before any
format freeze.
(4)~Aggregating or compressing spend proofs; at 71\,KiB the compact profile
is deployable server-side but remains the dominant bandwidth cost.
(5)~Hidden spend amounts, which would require in-circuit range checks and
re-open the balance-width trade-off.

\begin{thebibliography}{99}\small

\bibitem{Beullens21}
W.~Beullens.
\emph{MAYO: Practical Post-Quantum Signatures from Oil-and-Vinegar Maps}.
SAC 2021.

\bibitem{Beullens22}
W.~Beullens.
\emph{Breaking Rainbow Takes a Weekend on a Laptop}.
CRYPTO 2022.

\bibitem{MAYOspec}
W.~Beullens, F.~Campos, S.~Celi, B.~Hess, M.~J.~Kannwischer.
\emph{MAYO: Algorithm Specification (Round-2 document, February 5, 2025)}.
NIST PQC Additional Digital Signatures. This work pins the Round-2
specification; MAYO advanced to Round 3 in May 2026.
\url{https://pqmayo.org/resources/}.

\bibitem{VOLEitH}
C.~Baum, L.~Braun, C.~Delpech de Saint Guilhem, M.~Kloo{\ss}, E.~Orsini,
L.~Roy, P.~Scholl.
\emph{Publicly Verifiable Zero-Knowledge and Post-Quantum Signatures from
VOLE-in-the-Head}.
CRYPTO 2023.

\bibitem{FAEST}
C.~Baum, W.~Beullens, L.~Braun, C.~Delpech de Saint Guilhem, M.~Kloo{\ss},
C.~Majenz, S.~Mukherjee, E.~Orsini, S.~Ramacher, C.~Rechberger, L.~Roy,
P.~Scholl.
\emph{FAEST: Algorithm Specification, Version 2.0 (Round-2 document)}.
NIST PQC Additional Digital Signatures. This work follows the Round-2
(v2.0) design; FAEST advanced to Round 3 in May 2026.
\url{https://faest.info}.

\bibitem{OneTree}
C.~Baum, W.~Beullens, S.~Mukherjee, E.~Orsini, S.~Ramacher, C.~Rechberger,
L.~Roy, P.~Scholl.
\emph{One Tree to Rule Them All: Optimizing GGM Trees and OWFs for
Post-Quantum Signatures}.
ASIACRYPT 2024.

\bibitem{QuickSilver}
K.~Yang, P.~Sarkar, C.~Weng, X.~Wang.
\emph{QuickSilver: Efficient and Affordable Zero-Knowledge Proofs for
Circuits and Polynomials over Any Field}.
ACM CCS 2021.

\bibitem{Wolverine}
C.~Weng, K.~Yang, J.~Katz, X.~Wang.
\emph{Wolverine: Fast, Scalable, and Communication-Efficient Zero-Knowledge
Proofs for Boolean and Arithmetic Circuits}.
IEEE S\&P 2021.

\bibitem{MacNCheese}
C.~Baum, A.~J.~Malozemoff, M.~B.~Rosen, P.~Scholl.
\emph{Mac'n'Cheese: Zero-Knowledge Proofs for Boolean and Arithmetic
Circuits with Nested Disjunctions}.
CRYPTO 2021.

\bibitem{SoftSpoken}
L.~Roy.
\emph{SoftSpokenOT: Quieter OT Extension from Small-Field Silent VOLE in the
Minicrypt Model}.
CRYPTO 2022.

\bibitem{GGM86}
O.~Goldreich, S.~Goldwasser, S.~Micali.
\emph{How to Construct Random Functions}.
Journal of the ACM 33(4), 1986.

\bibitem{UOV}
A.~Kipnis, J.~Patarin, L.~Goubin.
\emph{Unbalanced Oil and Vinegar Signature Schemes}.
EUROCRYPT 1999.

\bibitem{FIPS202}
NIST.
\emph{SHA-3 Standard: Permutation-Based Hash and Extendable-Output
Functions}.
FIPS PUB 202, 2015.

\bibitem{BHT}
G.~Brassard, P.~H{\o}yer, A.~Tapp.
\emph{Quantum Cryptanalysis of Hash and Claw-Free Functions}.
LATIN 1998. arXiv:quant-ph/9705002.

\bibitem{AaronsonShi}
S.~Aaronson, Y.~Shi.
\emph{Quantum Lower Bounds for the Collision and the Element Distinctness
Problems}.
Journal of the ACM 51(4), 2004.

\bibitem{Chaum82}
D.~Chaum.
\emph{Blind Signatures for Untraceable Payments}.
CRYPTO 1982.

\bibitem{CL01}
J.~Camenisch, A.~Lysyanskaya.
\emph{An Efficient System for Non-transferable Anonymous Credentials with
Optional Anonymity Revocation}.
EUROCRYPT 2001.

\bibitem{CMZ14}
M.~Chase, S.~Meiklejohn, G.~Zaverucha.
\emph{Algebraic MACs and Keyed-Verification Anonymous Credentials}.
ACM CCS 2014.

\bibitem{PrivacyPass}
A.~Davidson, I.~Goldberg, N.~Sullivan, G.~Tankersley, F.~Valsorda.
\emph{Privacy Pass: Bypassing Internet Challenges Anonymously}.
Proceedings on Privacy Enhancing Technologies 2018(3), 164--180.

\bibitem{zkcreds}
M.~Rosenberg, J.~White, C.~Garman, I.~Miers.
\emph{zk-creds: Flexible Anonymous Credentials from zkSNARKs and Existing
Identity Infrastructure}.
IEEE S\&P 2023.

\bibitem{BLNS23}
J.~Bootle, V.~Lyubashevsky, N.~K.~Nguyen, A.~Sorniotti.
\emph{A Framework for Practical Anonymous Credentials from Lattices}.
CRYPTO 2023.

\bibitem{BLNSblind}
W.~Beullens, V.~Lyubashevsky, N.~K.~Nguyen, G.~Seiler.
\emph{Lattice-Based Blind Signatures: Short, Efficient, and Round-Optimal}.
ACM CCS 2023.

\bibitem{PoMFRIT}
C.~Baum, M.~Beckmann, W.~Beullens, S.~Mukherjee, C.~Rechberger.
\emph{Concretely Efficient Blind Signatures Based on VOLE-in-the-Head
Proofs and the MAYO Trapdoor (PoMFRIT)}.
USENIX Security 2026. IACR ePrint 2026/109.

\bibitem{KS}
J.~Katz, S.~Schlesinger.
\emph{Anonymous Credit Tokens}.
IETF CFRG Internet-Draft draft-schlesinger-cfrg-act (work in progress),
2025; design document and Rust implementation,
\url{https://github.com/SamuelSchlesinger/anonymous-credit-tokens}
(accessed July 2026).

\end{thebibliography}

\end{document}
